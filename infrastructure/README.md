# Infrastructure

`compose.yaml` is the standard cross-platform Docker Compose installation and
local development environment. It builds VynodeArr and starts private bundled
movie and television engines with persistent named volumes.
`Dockerfile.unraid` produces the self-contained Linux x86-64 release image.
Community Applications metadata lives under `templates/`, and the Windows
Docker Desktop launcher lives under `distribution/windows/`.
